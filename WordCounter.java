import java.io.IOException;

/**

The WordCounter class contains the main method that 
starts the application. When the application is started, a new
Controller object is created, which in turn creates 
the WordCounterGUI object and displays it on the screen.
*/

public class WordCounter {
  public static void main(String[] args) throws IOException {

    Controller wordCounterApp = new Controller();
  }

}